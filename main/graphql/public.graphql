type Query {
    Document: [Document] @addArgumentFromContext(name: "email", contextKey: "email")
}

schema {
    query: Query
}

type User {
    uuid: String!
    email: String!
    documents: [Document]
}

type Document {
    uuid: String!
    name: String!
    owner: User
}